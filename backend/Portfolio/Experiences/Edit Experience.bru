meta {
  name: Edit Experience
  type: http
  seq: 2
}

patch {
  url: http://localhost:3000/experiences/1
  body: json
  auth: inherit
}

body:json {
  {
    "challenge":"Applying (DRY) Principle",
    "solution":"Abstract Repo for CRUD operations and all repos extended it"
  }
}

settings {
  encodeUrl: true
}
